# SunFutures API Contract

This folder is the shared contract between clients and the API.

- `forecast.request.example.json`
- `forecast.response.example.json`

Keep these in sync with the FastAPI `ForecastRequest` / `ForecastResponse` models.
